{
  "name": "digipin-micro-saas",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "nodemon --exec ts-node src/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npm run build && pm2 restart ecosystem.config.js --env production",
    "deploy:fresh": "npm run build && pm2 start ecosystem.config.js --env production",
    "logs": "pm2 logs digipin-micro-saas",
    "monitor": "./scripts/monitor.sh",
    "backup": "./scripts/backup.sh",
    "manage-keys": "node scripts/manage-keys.js",
    "db:migrate": "node -e \"require('./src/database/connection').default.getInstance().runMigrations()\"",
    "health": "curl -s http://localhost:3000/health | jq ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@fastify/helmet": "^13.0.1",
    "@fastify/rate-limit": "^10.3.0",
    "@fastify/swagger": "^9.5.1",
    "@fastify/swagger-ui": "^5.2.3",
    "@types/node": "^24.5.2",
    "better-sqlite3": "^12.2.0",
    "digipin": "^0.0.1",
    "fastify": "^5.6.0",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13"
  }
}
